export enum DateFilterType {
    JOIN = 'joindate',
    EXPIRY = 'expirydate',
    UPDATE = 'updatedate',
    REMINDER = 'reminderdate',
    LASTTRANSACTION = 'lastTransactiondate',
    DELETE = 'deletedate',
}